<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "App Assist" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" integrity="sha512-..." crossorigin="anonymous" referrerpolicy="no-referrer" />

    <!-- ✅ Bootstrap CSS CDN -->
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css", rel: "stylesheet" %>
    <!-- Seus estilos locais -->
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% unless current_page?("/users/sign_in") %>
      <!-- Navbar com botão do drawer -->
      <nav class="navbar navbar-light bg-light shadow-sm">
        <div class="container-fluid">
          <button class="btn btn-outline-primary" type="button" data-bs-toggle="offcanvas" data-bs-target="#drawerMenu">
            <i class="fas fa-bars"></i>
          </button>
          <a class="navbar-brand ms-3" href="#">Meu App</a>
        </div>
      </nav>

      <!-- Drawer Menu (Offcanvas) -->
      <div class="offcanvas offcanvas-start" tabindex="-1" id="drawerMenu" aria-labelledby="drawerMenuLabel">
        <div class="offcanvas-header">
          <h5 class="offcanvas-title" id="drawerMenuLabel"><i class="fas fa-bars me-2"></i> Menu</h5>
          <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Fechar"></button>
        </div>
        <div class="offcanvas-body">
          <ul class="nav flex-column">
            <li class="nav-item">
              <button class="btn btn-link nav-link" onclick="window.location.href='/'">
                <i class="fas fa-home me-2"></i> Início
              </button>
            </li>
            <li class="nav-item">
              <button class="btn btn-link nav-link" onclick="window.location.href='/clientes'">
                <i class="fas fa-id-card me-2"></i> Clientes
              </button>
            </li>
            <li class="nav-item">
              <button class="btn btn-link nav-link" onclick="window.location.href='/contato'">
                <i class="fas fa-envelope me-2"></i> Contato
              </button>
            </li>
          </ul>
        </div>
      </div>
    <% end %>

    <% if user_signed_in? %>
      <div class="dropdown position-absolute top-0 end-0 m-3">
        <a href="/" class="d-flex align-items-center text-decoration-none dropdown-toggle" id="avatarDropdown" data-bs-toggle="dropdown" aria-expanded="false">
          <img src="https://www.gravatar.com/avatar/<%= Digest::MD5.hexdigest(current_user.email) %>?s=40&d=identicon" alt="avatar" class="rounded-circle me-2">
          <strong><%= current_user.email.split("@").first.capitalize %></strong>
        </a>
        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="avatarDropdown">
          <li><%= button_to "Sair", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
        </ul>
      </div>
    <% end %>

    <%= yield %>

    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js", defer: true %>
  </body>
</html>
